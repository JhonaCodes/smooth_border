name: smooth_border
description: "iOS-style continuous corner radius (squircle) for Flutter. A ShapeBorder that works with any widget — Container, Card, Material, ClipPath, and more."
version: 1.0.3
homepage: https://github.com/JhonaCodes/smooth_border.git
repository: https://github.com/JhonaCodes/smooth_border

environment:
  sdk: ^3.10.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  alchemist: ^0.13.0
